apiVersion: v1
kind: Service
metadata:
  name: server-service
spec:
  selector:
    app: inventory-server
  ports:
  - protocol: TCP
    port: 3001
    targetPort: 3001
    nodePort: 30001
  type: NodePort

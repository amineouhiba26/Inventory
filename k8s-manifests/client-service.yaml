apiVersion: v1
kind: Service
metadata:
  name: client-service
spec:
  selector:
    app: inventory-client
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30002
  type: NodePort
